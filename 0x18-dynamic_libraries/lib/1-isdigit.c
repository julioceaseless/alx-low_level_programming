#include "main.h"

/**
 * _isdigit - checks for digit
 *
 * @c: character to check
 * Return: 1 if c is uppercase else 0
 */
int _isdigit(int c)
{
	if (isdigit(c))
	{
		return (1);

	}
	else
	{
		return (0);

	}
}
