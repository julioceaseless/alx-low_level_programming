#include "main.h"

/**
 * _isupper - checks for uppercase letter
 *
 * @c: character to check
 * Return: 1 if c is uppercase else 0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);

	}
	else
	{
		return (0);

	}
}
